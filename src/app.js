const express = require('express');

const app = express();
app.use(express.json());

const express = require('express');
const { Cat } = require('../models');

app.post('/cat', (req, res) => {

})

module.exports = app;
